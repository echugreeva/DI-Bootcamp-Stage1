-- SELECT count(1) FROM actor
-- INSERT INTO actor VALUES ( 201 ,'','') // won't give input all blanks, integer fieled was needed to proceed, NULL was also not allowed as input, empty datastamp automatically added current date

-- other fields remained empty
