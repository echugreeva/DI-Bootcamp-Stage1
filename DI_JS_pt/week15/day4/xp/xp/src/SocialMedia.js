const SocialMedia = (props) =>{
    // console.log(`props: ${props.item}`)

    return (
        
        <div>
        <p>{props.item}</p>
        </div>
    )      
    
}

export default SocialMedia